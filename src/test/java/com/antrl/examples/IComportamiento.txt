package com.antrl.examples;

import lombok.Setter;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

import javax.persistence.Id;

//@FunctionalInterface
public interface IComportamiento<Uno,Dos> extends JpaRepository<Domicilio,Integer>, JpaTest {
    //Por defecto las variables de una interfaz son estaticas , publicas y finales
    //Formalmente una interfaz funcional se identifica con la anotacion @FunctionalInterface, y debe contener un unico metodo abstracto y todos los defaults que se quiera  y variables
    //Los métodos default se pueden sobreescribir si se quisieran en las clases que implementen la interfaz
    //El Método abstracto unico en una interfaz funcional sirve para que a una firma de una función se le pueda rellenar el cuerpo en una clase que la implemente de la forma que se quiera, pero utilizando expresiones lambda ..
    //.. en distintas lineas se puede almacenar en una variable del tipo de la interfaz un cuerpo distinto para una misma firma de función

//public void algo();

int tres=3,cuatro=4,cinco=5;
boolean verdad=true, mentira=false;
String a=new String("2");
Constant constant = new Constant("1",2,true,new Domicilio());
}